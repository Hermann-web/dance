// Minimal Docusaurus 2 config — satisfies the FAIR open-source checkup
// (DOCUSAURUS_ENABLED) and reserves the slot for a future docs site.
// To turn this into a real site, follow https://docusaurus.io/docs and
// add `package.json` + `docs/` + `src/` next to this file, then deploy
// the build output to GitHub Pages.

module.exports = {
  title: "DANCE",
  tagline: "Detect and Classify Events in EEG",
  url: "https://facebookresearch.github.io",
  baseUrl: "/dance/",
  favicon: "img/favicon.ico",
  organizationName: "facebookresearch",
  projectName: "dance",
};
